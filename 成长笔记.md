# 张强的 Vibe Coding 成长笔记

> 从"技术小白，不会写代码"出发，记录每一次能力跃迁。
> 自动维护，每次重要协作后更新。

---

## 能力进阶总览

```
阶段一：能跑起来          ██░░░░░░░░  会用工具、启服务、部署
阶段二：能改起来          ████░░░░░░  改代码、加功能、理解前后端
阶段三：能建起来          ██████░░░░  从零搭 Agent、RAG 知识库
阶段四：能调起来          ████████░░  系统评估、定位根因、精准修复
阶段五：能设计起来        ██████████  独立设计架构方案、预判问题
```

---

## 2026-06-14 — Agent Eval 调试：学会根因分析

### 做了什么
- 跑了 25 道评测题的完整 Eval 评估
- 从 TF-IDF 升级到两阶段检索（TF-IDF 粗筛 + DeepSeek 精排）
- 修复了三个系统级问题：检索污染、模型幻觉、引用集中

### 核心收获
1. **"降模型能力"不是修复方案**
   - 之前遇到幻觉，第一反应是换弱模型、降温度
   - 意识到这是"治标不治本"——应该找到模型推理能力用错地方的根因
   - 真正的问题是**身份混淆**：预训练数据里其他叫"张强"的人污染了回答

2. **Agent 调试方法论**
   - 回答出问题 → 查检索来源 → 查来源内容 → 判断是检索/知识库/Prompt 哪层的问题
   - **核心心法："先怀疑自己写的 Prompt，再怀疑模型"**
   - 很多时候模型"说错了"，其实是 Prompt 让它这么说的

3. **Eval 是迭代的锚点**
   - 没有量化评估，调优就是在"凭感觉"
   - 跑一遍 Eval → 找 Badcase → 修根因 → 再跑一遍验证
   - 两轮 Eval 对比是最有说服力的证据

### 技能解锁
- 🔓 会写批量评测脚本，用 API 跑 25 道题
- 🔓 理解 TF-IDF vs 语义检索的 trade-off
- 🔓 能识别"身份混淆"类问题并加 Prompt 约束解决

---

## 2026-06-11 — 知识库自动化 + 粘贴格式保留

### 做了什么
- 推送部署时自动把文章导出到知识库（`自动导入文章-xxx.md`）
- 接入 Turndown 库，粘贴公众号文章时 HTML → Markdown 保留格式
- 解决知识库热重载问题：索引重建后通知后端自动刷新

### 核心收获
1. **输入端的问题要在输入端解决**
   - 一开始想在导出时用 AI 还原格式——思路反了
   - 格式在粘贴进 textarea 那一刻就丢了，后面怎么猜都猜不准
   - 正确做法：拦截 paste 事件，读剪贴板 HTML，转 Markdown

2. **数据流要形成闭环**
   - 之前：粘贴文章 → AI 分析 → 全文丢弃，只保留摘要
   - 修复后：粘贴 → AI 分析 → 推送部署 → 自动写知识库 → Git 同步 → Render 部署
   - 好的产品设计要让数据自己流动，不需要人手动搬运

3. **服务间的协同**
   - 三个服务（8080 静态 / 8081 代理 / 8000 后端）各司其职
   - proxy 写完文件后通知后端热重载——服务之间要"对话"

### 技能解锁
- 🔓 理解 CORS 跨域 + API 代理的作用
- 🔓 会写 HTML 端 paste 事件拦截
- 🔓 理解索引热加载的概念

---

## 2026-06-05 至 2026-06-10 — V2 数字分身 Agent 搭建

### 做了什么
- 搭建 RAG 后端（FastAPI + TF-IDF 检索 + DeepSeek 生成）
- 建知识库：个人介绍、简历、AI 知识积累、GEO 文章、白话 AI 文章
- 写 System Prompt：角色定义、能力边界、产品思维框架、语气人设
- 前端聊天抽屉（流式逐字输出、思考过程折叠）
- 管理后台：System Prompt 编辑器、会话记录查看

### 核心收获
1. **RAG 的基本原理**
   - 检索增强生成 = 文档切片 → 向量化 → 存储 → 查询时检索 → 喂给模型
   - 切片策略决定检索质量：按 `##` 标题切比按字数切更保留语义
   - 向量存储只是"数学坐标"，本质是在找语义相近的内容

2. **System Prompt 是 Agent 的灵魂**
   - 不只是"你是XXX"——要定义角色、边界、思维框架、语气
   - 好的 Prompt 是产品经理的 PRD：越清晰，交付越可控

3. **工程的现实约束**
   - ChromaDB 吃 400MB 内存，Render 免费版只有 512MB
   - 降级到 sklearn TF-IDF，50MB 就能跑——够用就好
   - 技术选型先看约束，再选方案

### 技能解锁
- 🔓 从零理解 RAG 架构
- 🔓 能写 System Prompt 并理解其影响力
- 🔓 理解"切片策略对检索质量的影响"

---

## 2026-05-27 至 2026-06-04 — 管理后台搭建

### 做了什么
- 从静态页面扩展到管理后台（admin.html）
- API 代理（proxy.py）解决 CORS 跨域
- 配置 DeepSeek API Key + GitHub Token
- 一键推送部署到 Vercel

### 核心收获
1. **前端不只是展示，可以做工具**
   - 管理后台本质是"给自己用的产品"
   - 流程：粘贴文章 → AI 分析摘要标签 → 生成数据文件 → Git 推送 → 自动部署

2. **API Key 的安全隔离**
   - 前端不暴露 Key → 代理层转发 → 服务端调用
   - 用户看到的网页里没有 Key，Key 存在代理服务器上

### 技能解锁
- 🔓 理解前后端分离 + API 代理
- 🔓 会用 Git（add/commit/push）
- 🔓 理解 CI/CD 自动部署（Vercel/Render）

---

## 更早 — 从零开始的个人网站

### 做了什么
- 纯静态 HTML + CSS 个人主页
- Vercel 部署上线
- GitHub 仓库管理

### 核心收获
- 第一次"写代码并让全世界看到"
- 理解静态网页的工作方式

---

## 我的"啊哈时刻" 💡

| 时刻 | 领悟 |
|:--|:--|
| 2026-06-14 | 模型说错了，先别怀疑模型，先怀疑自己写的 Prompt |
| 2026-06-14 | 降模型能力是偷懒，找到根因、加精准约束才是真本事 |
| 2026-06-11 | 问题在输入端解决，不要指望后面"补救" |
| 2026-06-10 | TF-IDF 虽然简陋，但够用就是好方案 |
| 2026-06-05 | Agent 不只是"聊天机器人"，是产品思维的活体演示 |
| 2026-06-16 | Prompt 写对了但发错了——多条 System 消息会互相淹没，交付方式和内容同等重要 |

---

*最后更新：2026-06-16*
*下次更新时自动追加新条目*

---

## 2026-06-14（续）— 环境不一致：测了半天测的是生产环境

### 发生了什么
调 System Prompt 调了好几轮，每次测试聊天都感觉"没生效"。最后发现——网页聊天窗口的 API 地址写死了 Render 线上服务，本地改的 Prompt 根本没被用到。测了半天，测的是别人。

### 核心收获
1. **"环境不一致"是最容易被忽视的 bug**
   - 代码逻辑没问题、Prompt 没问题、后端没问题
   - 但测试入口指错了环境——等于白测
   - 这类 bug 没有报错信息，只能靠排查发现

2. **本地和线上要自动切换**
   - 之前 `API_URL` 写死 Render 地址
   - 改成 `window.location.hostname === 'localhost'` 自动判断
   - 原则：永远不要让人类手动切换环境，人会忘

3. **部署不是"一键推送"就完了**
   - 当前管理后台的「推送部署」只提交 `articles-data.js` + `knowledge/`
   - System Prompt、前端页面、后端代码的改动需要手动 git commit + push
   - 创建了《部署流程文档》，列出每种场景的完整部署步骤

### 技能解锁
- 🔓 理解本地/生产环境隔离
- 🔓 会排查"没报错但不对"的问题
- 🔓 能写出覆盖全链路的部署清单

---

## 2026-06-16 — 三条 System 消息互相打架：Prompt 的"交付方式"也会出错

### 发生了什么
Agent 出现严重幻觉——把"去哪儿网"、另一个"张强"的经历编造进回答里。而且知识库的原始内容泄漏到了回答中。折腾了三个小时才定位到根因。

### 核心收获
1. **多条 System 消息是坑**
   - 之前代码发了三条 System 消息：①人格 Prompt ②知识库 ③对话历史
   - DeepSeek 对多条 System 消息的权重处理不确定——可能只认真最后一条，忽略了人格约束
   - **修复：合并为一条 System 消息**，把人格 + 知识 + 历史全部装进去，模型无法选择性忽略

2. **Prompt 不只写什么重要，怎么发给模型也重要**
   - Prompt 内容本身是对的，但"交付方式"让模型选择性忽略了
   - 知识格式 `--- 知识片段 ---` 这种 Markdown 标记会被 DeepSeek 复述到回答里
   - 改成简洁的 `[参考N · 来源]` 格式，并加显式禁止输出指令

3. **改代码后要确认服务真的重启了**
   - 改完代码、杀掉旧进程、启动新进程——三步缺一不可
   - 浏览器也要强制刷新（Cmd+Shift+R），避免缓存干扰

### 技能解锁
- 🔓 理解多条 System 消息的权重问题
- 🔓 会定位"Prompt 格式泄漏"类 bug
- 🔓 掌握"合并 System 消息"的调试技巧
