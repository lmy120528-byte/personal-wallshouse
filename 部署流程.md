# 部署流程文档

> 每次本地修改后，哪些需要部署到线上？每一项都不能漏。

---

## 架构总览

```
GitHub (github.com/lmy120528-byte/personal-wallshouse)
    │
    ├── 自动触发 ──→ Vercel (personal-wallshouse.vercel.app)
    │                   静态文件：HTML/CSS/JS/图片
    │
    └── 自动触发 ──→ Render (personal-wallshouse.onrender.com)
                        RAG 后端：Python FastAPI
```

---

## 部署清单

### 1. Vercel — 静态前端

| 文件 | 说明 | 部署方式 |
|:--|:--|:--|
| `index.html` | 主页面 + 聊天抽屉 | Git push → Vercel 自动部署 |
| `style.css` | 全局样式 | Git push → Vercel 自动部署 |
| `admin.html` | 管理后台 | Git push → Vercel 自动部署 |
| `articles.html` | 文章列表页 | Git push → Vercel 自动部署 |
| `projects.html` | 项目展示页 | Git push → Vercel 自动部署 |
| `hero-bg.js` | 背景动画 | Git push → Vercel 自动部署 |
| `articles-data.js` | 文章数据中心 | **管理后台「推送部署」自动提交** |
| `images/*` | 图片资源 | Git push → Vercel 自动部署 |

### 2. Render — RAG 后端

| 文件 | 说明 | 部署方式 |
|:--|:--|:--|
| `rag-backend/main.py` | FastAPI 服务入口 | Git push → Render 自动部署 |
| `rag-backend/retriever.py` | 两阶段检索 | Git push → Render 自动部署 |
| `rag-backend/ingest.py` | 知识库入库脚本 | Git push → Render 自动部署 |
| `rag-backend/system_prompt.txt` | ⚠️ Agent 人格定义 | **需手动 git commit + push** |
| `rag-backend/knowledge/*.md` | 知识库文章 | **管理后台「推送部署」自动提交** |
| `rag-backend/knowledge/*.txt` | 知识库（手动维护） | Git push → Render 自动部署 |
| `rag-backend/requirements.txt` | Python 依赖 | Git push → Render 自动部署 |
| `requirements.txt` | 根目录依赖 | Git push → Render 自动部署 |
| `rag-backend/embeddings.pkl` | Embedding 索引 | ❌ 不部署（.gitignore，Render 自动构建） |

### 3. 代理服务（仅本地）

| 文件 | 说明 | 部署方式 |
|:--|:--|:--|
| `proxy.py` | API 代理（DeepSeek + 部署） | 仅本地运行，不部署 |

---

## 当前自动部署范围

管理后台点「推送部署」时，自动提交的文件：

```
✅ articles-data.js          （文章数据）
✅ rag-backend/knowledge/    （自动导入的文章 .md 文件）
```

**以下文件「推送部署」不会自动提交，需要手动 git 操作：**

```
❌ system_prompt.txt         改 Prompt 后
❌ index.html                改聊天 UI 后
❌ admin.html                改管理后台后
❌ style.css                 改样式后
❌ rag-backend/main.py       改后端逻辑后
❌ rag-backend/retriever.py  改检索逻辑后
❌ proxy.py                  改代理逻辑后
```

---

## 不同场景的部署步骤

### 场景 A：发新文章
```
管理后台 → 粘贴文章 → AI 分析 → 推送部署
→ 自动提交 articles-data.js + knowledge/*.md + git push
→ Vercel ✅  Render ✅（含知识库热重载）
```
无需手动操作。

### 场景 B：改 System Prompt
```
1. 管理后台 → System Prompt 标签 → 编辑 → 保存（本地立即生效验证）
2. 验证通过后，手动提交：
   cd personal-site
   git add rag-backend/system_prompt.txt
   git commit -m "更新 System Prompt"
   git push origin main
→ Vercel ✅（无影响） Render ✅（重启后生效）
```

### 场景 C：改前端页面（index.html / style.css 等）
```
1. 本地编辑 + 验证（http://localhost:8080）
2. 手动提交：
   git add index.html style.css ...
   git commit -m "更新前端页面"
   git push origin main
→ Vercel ✅（自动部署静态文件） Render ✅（无影响）
```

### 场景 D：改后端代码（main.py / retriever.py 等）
```
1. 本地编辑 + 验证（http://localhost:8000）
2. 手动提交：
   git add rag-backend/main.py rag-backend/retriever.py ...
   git commit -m "更新后端逻辑"
   git push origin main
→ Render ✅（自动重新部署）
```

---

## 常见踩坑

| 坑 | 原因 | 避免方法 |
|:--|:--|:--|
| 本地改了 Prompt，线上没生效 | 改了本地文件但没 git push | 场景 B 流程 |
| 改了前端代码，网站没变化 | 同上 | 场景 C 流程 |
| 本地测完没问题，线上出 bug | index.html 的 API_URL 指向不同后端 | 确认 API_URL 自动切换逻辑 |
| 知识库文章检索不到 | Render 上索引没重建 | Render 启动时自动 run ingest.py |

---

## 本地开发环境

```bash
# 终端 1：静态服务
python3 -m http.server 8080

# 终端 2：API 代理
python3 proxy.py

# 终端 3：RAG 后端
cd rag-backend && python3 main.py
```

三个服务都必须在跑，管理后台和聊天才能正常工作。

---

*最后更新：2026-06-14*
