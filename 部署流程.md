# 部署流程文档

> 每次本地修改后，哪些需要部署到线上？每一项都不能漏。

---

## 架构总览（2026-09-19 起）

```
GitHub (github.com/lmy120528-byte/personal-wallshouse)
    │
    │  git push（代码存档 + 版本管理）
    ▼
本地 Mac
    │
    │  scp 同步
    ▼
腾讯云服务器 124.223.77.181（与奶粉 Agent 共用，nginx 按路径切分）
    ├── /personal/        → 静态文件   /home/agentuser/personal-site/
    ├── /personal/admin   → 管理后台   （同上目录 admin.html）
    └── /personal/api/    → RAG 后端   systemd: personal-rag（端口 8001）
                                          /home/agentuser/personal-rag/
```

---

## 部署清单

### 1. 静态前端 → /home/agentuser/personal-site/

| 文件 | 说明 | 部署方式 |
|:--|:--|:--|
| `index.html` | 主页面 + 聊天抽屉 | scp 同步（无需重启） |
| `style.css` | 全局样式 | scp 同步 |
| `admin.html` | 管理后台 | scp 同步 |
| `articles-data.js` | 文章数据中心 | 管理后台「推送部署」自动提交 → scp 同步 |
| `hero-v2.js` / `js/*` | 动效脚本与库 | scp 同步 |
| `images/*` | 图片资源 | scp 同步 |

### 2. RAG 后端 → /home/agentuser/personal-rag/（同步后需重启服务）

| 文件 | 说明 | 部署方式 |
|:--|:--|:--|
| `rag-backend/main.py` | FastAPI 服务入口 | scp 同步 + `systemctl restart personal-rag` |
| `rag-backend/retriever.py` | 检索逻辑 | 同上 |
| `rag-backend/ingest.py` | 知识库入库脚本 | 同上 |
| `rag-backend/system_prompt.txt` | ⚠️ Agent 人格定义 | 手动 commit + scp + 重启 |
| `rag-backend/knowledge/*.md` | 知识库文章 | 管理后台「推送部署」自动提交 → scp + 重新 ingest |
| `rag-backend/embeddings.pkl` | Embedding 索引 | 重新 ingest 后 scp（❌ 不推 GitHub） |

### 3. 代理服务（仅本地）

| 文件 | 说明 | 部署方式 |
|:--|:--|:--|
| `proxy.py` | API 代理（DeepSeek + 部署） | 仅本地运行，不部署 |

---

## 标准同步流程（所有场景通用）

```bash
# 1. 提交并推送 GitHub
git add <改动的文件>
git commit -m "描述本次改动"
git push origin main

# 2. 同步静态文件到服务器
scp index.html style.css admin.html agentuser@124.223.77.181:/home/agentuser/personal-site/

# 3. 同步后端文件并重启服务
scp rag-backend/main.py agentuser@124.223.77.181:/home/agentuser/personal-rag/
ssh agentuser@124.223.77.181 "sudo systemctl restart personal-rag"

# 4. 验证
curl -I http://124.223.77.181/personal/
```

---

## 同步注意事项（重要）

1. **服务器目录不是 git 仓库**：更新靠 scp 传改动的文件，不要整包重拷。
   服务器在国内访问 GitHub 不稳定（迁移时 git clone 曾超时），scp 走 Mac→服务器直连，是最可靠的同步方式。
2. **macOS 拷文件防 ._ 垃圾**：tar 打包上传时必须加 `COPYFILE_DISABLE=1`（否则每个文件
   都会带一个 `._` 开头的小跟班垃圾文件，且会被 nginx 公开访问）：
   ```bash
   COPYFILE_DISABLE=1 tar czf site.tar.gz personal-site/
   ```
   scp 直接传单个文件不会产生 ._ 垃圾，可放心使用。
   （2026-09-19 已清理过一次：personal-site 16 个 + personal-rag 22 个 ._ 文件。）
3. **同步后必须验证**：静态文件 curl 看 HTTP 200；后端改动重启服务后用真实请求测（不能只看 systemctl active）。

---

## 不同场景的部署步骤

### 场景 A：发新文章
```
管理后台 → 粘贴文章 → AI 分析 → 推送部署
→ 自动提交 articles-data.js + knowledge/*.md + git push
→ 手动：scp 同步 + 服务器重新 ingest 重建索引
```

### 场景 B：改 System Prompt
```
1. 管理后台 → System Prompt 标签 → 编辑 → 保存（本地立即生效验证）
2. git commit + push + scp system_prompt.txt
3. 服务器重启服务后生效
```

### 场景 C：改前端页面（index.html / style.css 等）
```
1. 本地编辑 + 验证（http://localhost:8080）
2. git commit + push
3. scp 同步到 /home/agentuser/personal-site/（无需重启，刷新即生效）
```

### 场景 D：改后端代码（main.py / retriever.py 等）
```
1. 本地编辑 + 验证（http://localhost:8000）
2. git commit + push
3. scp 同步到 /home/agentuser/personal-rag/
4. ssh 执行 systemctl restart personal-rag
5. 用真实请求验证（curl 或页面聊天）
```

---

## 常见踩坑

| 坑 | 原因 | 避免方法 |
|:--|:--|:--|
| 改了前端，网站没变化 | 忘了 scp 同步 | 场景 C 流程第 3 步 |
| 改了后端，聊天还是旧的 | 忘了重启服务 | 场景 D 流程第 4 步 |
| 服务显示 active 但聊天卡死 | 依赖缺失/网络问题，非 Key 问题 | 看日志 `journalctl -u personal-rag -n 50` 分层排查 |
| 服务器访问不了 HuggingFace/GitHub | 国内网络 | 模型走 hf-mirror.com，代码用 scp 手动传 |
| 知识库文章检索不到 | 服务器索引没重建 | 重新 ingest 后同步 embeddings.pkl |
| 访问 /personal 跳到奶粉 Agent | nginx 兜底规则（缺斜杠） | 已修复：精确匹配重定向 |
| .env / .venv 泄露 | 放在 web 根目录会被下载 | 后端必须在 web 根目录之外 |

---

## 本地开发环境

```bash
# 终端 1：静态服务
python3 -m http.server 8080

# 终端 2：API 代理
python3 proxy.py

# 终端 3：RAG 后端
cd rag-backend && python3 main.py
```

---

## 历史架构（已停用，2026-09-19）

- 旧部署：Vercel（静态前端）+ Render（RAG 后端），需开 VPN 访问
- 已停用下线；知识库中"搭建复盘"系列文章描述的 Vercel/Render 为历史记录

---

*最后更新：2026-09-19*
